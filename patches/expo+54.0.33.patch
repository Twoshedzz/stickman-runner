diff --git a/node_modules/expo/src/async-require/fetchThenEvalJs.ts b/node_modules/expo/src/async-require/fetchThenEvalJs.ts
index 0000000..1111111 100644
--- a/node_modules/expo/src/async-require/fetchThenEvalJs.ts
+++ b/node_modules/expo/src/async-require/fetchThenEvalJs.ts
@@ -24,7 +24,36 @@ export function fetchThenEvalAsync(url: string): Promise<void> {
     }
 
     if (status === 200) {
-      // eslint-disable-next-line no-eval
-      return eval(body);
+      // Wait for Skia WASM before eval so CanvasKit chunks see __SKIA_WASM_BINARY__ (avoids Aborted() in segment 4/5).
+      const g = typeof globalThis !== 'undefined' ? (globalThis as { __SKIA_WASM_PROMISE__?: Promise<unknown> }) : null;
+      const wasmPromise = g?.__SKIA_WASM_PROMISE__;
+      const doEval = () => {
+        // eslint-disable-next-line no-eval
+        return eval(body);
+      };
+      // Retry up to two times on next frame(s) if eval throws Aborted (e.g. CanvasKit under load during collision/particles).
+      const doEvalWithRetry = (attempt = 0): Promise<void> => {
+        const maxRetries = 2;
+        try {
+          doEval();
+          return Promise.resolve();
+        } catch (e: any) {
+          const msg = e?.message ?? String(e);
+          if (msg.includes('Aborted') && attempt < maxRetries) {
+            return new Promise((resolve, reject) => {
+              requestAnimationFrame(() => {
+                doEvalWithRetry(attempt + 1).then(resolve).catch(reject);
+              });
+            });
+          }
+          return Promise.reject(e);
+        }
+      };
+      if (wasmPromise) {
+        return wasmPromise.then(doEvalWithRetry).catch(() => doEvalWithRetry());
+      }
+      return doEvalWithRetry();
     } else {
       // Format Metro errors if possible.
